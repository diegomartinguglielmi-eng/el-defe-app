(() => {
  const STORE='defe_comunicaciones_v1';
  const READ='defe_comunicaciones_leidas_v1';
  const API='https://el-defe-v5-production.up.railway.app';
  const seed=[
    {id:'bienvenida-comms',titulo:'Bienvenido a Comunicaciones',mensaje:'Este será el canal central de avisos del club. Vas a recibir aquí las novedades dirigidas a tus categorías y actividades.',tipo:'Información',audiencia:'Todo el club',fecha:new Date().toISOString(),remitente:'Defe'}
  ];
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const get=()=>{try{const x=JSON.parse(localStorage.getItem(STORE)||'null');return Array.isArray(x)?x:seed}catch{return seed}};
  const save=x=>localStorage.setItem(STORE,JSON.stringify(x));
  const reads=()=>{try{return new Set(JSON.parse(localStorage.getItem(READ)||'[]'))}catch{return new Set}};
  const saveReads=x=>localStorage.setItem(READ,JSON.stringify([...x]));
  function jwt(){for(const st of [localStorage,sessionStorage])for(let i=0;i<st.length;i++){const v=st.getItem(st.key(i))||'';const m=v.match(/eyJ[a-zA-Z0-9_-]+\.[a-zA-Z0-9_-]+\.[a-zA-Z0-9_-]+/);if(m)return m[0]}return null}
  function role(){try{const t=jwt();if(!t)return null;let p=t.split('.')[1].replace(/-/g,'+').replace(/_/g,'/');return JSON.parse(atob(p.padEnd(Math.ceil(p.length/4)*4,'='))).role}catch{return null}}
  const isAdmin=()=>role()==='admin';
  function css(){if(document.getElementById('defe-comms-css'))return;const s=document.createElement('style');s.id='defe-comms-css';s.textContent=`
  .dc-fab{position:fixed;right:14px;bottom:78px;z-index:9997;background:#40368f;color:#fff;border:0;border-radius:999px;padding:11px 15px;font-weight:800;box-shadow:0 5px 18px #0003}.dc-badge{display:inline-grid;place-items:center;background:#e11d48;color:#fff;border-radius:999px;min-width:20px;height:20px;padding:0 5px;margin-left:6px;font-size:11px}.dc-modal{position:fixed;inset:0;z-index:100000;background:#11182788;padding:16px;display:flex;align-items:center;justify-content:center}.dc-card{background:#fff;width:min(560px,100%);max-height:88vh;overflow:auto;border-radius:22px;padding:18px;box-shadow:0 20px 60px #0004}.dc-top{display:flex;justify-content:space-between;align-items:center;gap:10px}.dc-close,.dc-action{border:0;border-radius:11px;padding:10px 12px;font-weight:700}.dc-action{background:#40368f;color:#fff}.dc-item{border:1px solid #e5e7eb;border-radius:15px;padding:13px;margin:10px 0}.dc-item.unread{border-left:5px solid #40368f;background:#f8f7ff}.dc-meta{font-size:12px;color:#6b7280;margin:4px 0 8px}.dc-pill{font-size:11px;font-weight:800;border-radius:999px;background:#eeeefa;padding:4px 8px;display:inline-block;margin-right:5px}.dc-form input,.dc-form textarea,.dc-form select{box-sizing:border-box;width:100%;padding:11px;border:1px solid #d1d5db;border-radius:11px;margin:5px 0 11px;font:inherit}.dc-form label{font-size:12px;font-weight:800;color:#4b5563}`;document.head.appendChild(s)}
  function unread(){const r=reads();return get().filter(x=>!r.has(x.id)).length}
  function renderButton(){css();let b=document.querySelector('.dc-fab');if(!b){b=document.createElement('button');b.className='dc-fab';b.onclick=openInbox;document.body.appendChild(b)}const n=unread();b.innerHTML=`Comunicaciones${n?`<span class="dc-badge">${n}</span>`:''}`}
  function modal(html){document.querySelector('.dc-modal')?.remove();const m=document.createElement('div');m.className='dc-modal';m.innerHTML=`<div class="dc-card">${html}</div>`;document.body.appendChild(m);m.addEventListener('click',e=>{if(e.target===m)m.remove()});m.querySelector('[data-dc-close]')?.addEventListener('click',()=>m.remove());return m}
  function openInbox(){const data=get().sort((a,b)=>String(b.fecha).localeCompare(String(a.fecha))),r=reads();const items=data.map(x=>`<div class="dc-item ${r.has(x.id)?'':'unread'}" data-id="${esc(x.id)}"><div><span class="dc-pill">${esc(x.tipo)}</span><span class="dc-pill">${esc(x.audiencia)}</span></div><h3 style="margin:8px 0 3px">${esc(x.titulo)}</h3><div class="dc-meta">${new Date(x.fecha).toLocaleString('es-AR')} · ${esc(x.remitente||'Defe')}</div><div>${esc(x.mensaje)}</div>${r.has(x.id)?'':'<button class="dc-action" data-read style="margin-top:10px">Marcar como leída</button>'}</div>`).join('');const m=modal(`<div class="dc-top"><div><h2 style="margin:0">Comunicaciones</h2><div class="dc-meta">Avisos y mensajes del club</div></div><button class="dc-close" data-dc-close>✕</button></div>${isAdmin()?'<button class="dc-action" data-new>+ Nueva comunicación</button>':''}${items||'<p>No hay comunicaciones.</p>'}`);m.querySelector('[data-new]')?.addEventListener('click',openComposer);m.querySelectorAll('[data-read]').forEach(b=>b.onclick=()=>{const id=b.closest('[data-id]').dataset.id;const rr=reads();rr.add(id);saveReads(rr);renderButton();openInbox()})}
  function openComposer(){const m=modal(`<div class="dc-top"><h2 style="margin:0">Nueva comunicación</h2><button class="dc-close" data-dc-close>✕</button></div><form class="dc-form"><label>Título</label><input name="titulo" required maxlength="90"><label>Mensaje</label><textarea name="mensaje" required rows="5" maxlength="1200"></textarea><label>Prioridad / tipo</label><select name="tipo"><option>Información</option><option>Importante</option><option>Urgente</option><option>Evento / Convocatoria</option></select><label>Destinatarios</label><select name="audiencia"><option>Todo el club</option><option>Baby</option><option>Futsal</option><option>FEFI</option><option>LAAMBA</option><option>Argenliga</option><option>Superliga</option></select><button class="dc-action" type="submit">Publicar</button></form>`);m.querySelector('form').onsubmit=e=>{e.preventDefault();const f=new FormData(e.currentTarget);const data=get();data.unshift({id:'c-'+Date.now(),titulo:f.get('titulo'),mensaje:f.get('mensaje'),tipo:f.get('tipo'),audiencia:f.get('audiencia'),fecha:new Date().toISOString(),remitente:'Administración Defe'});save(data);m.remove();renderButton();openInbox()}}
  if(!localStorage.getItem(STORE))save(seed);
  renderButton();setInterval(renderButton,1500);
})();
