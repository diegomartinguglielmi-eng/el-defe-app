# redeploy 2026-09-12 competencias filtros v4 validado
[build]
  command = "bash netlify-build.sh"
  publish = "netlify-publish"

[build.environment]
  NODE_VERSION = "20"
  PYTHON_VERSION = "3.13"
  NETLIFY_SKIP_PYTHON = "true"

[functions]
  directory = "netlify-functions-disabled"
