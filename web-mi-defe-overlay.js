// El Defe · Mi Defe v5 · todas las selecciones seguidas visibles
(() => {
  if(window.__defeMiDefeLoaded)return;window.__defeMiDefeLoaded=true;
  const API='https://el-defe-v5-production.up.railway.app';
  const CFG='defe_mi_defe_config_v1';
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  function jwtFromValue(v){if(!v||typeof v!=='string')return null;const m=v.match(/eyJ[a-zA-Z0-9_-]+\.[a-zA-Z0-9_-]+\.[a-zA-Z0-9_-]+/);if(m)return m[0];try{const p=JSON.parse(v);if(p&&typeof p==='object')for(const x of Object.values(p)){const f=jwtFromValue(typeof x==='string'?x:JSON.stringify(x));if(f)return f}}catch(_){}return null}
  function token(){for(const st of [localStorage,sessionStorage])for(let i=0;i<st.length;i++){const t=jwtFromValue(st.getItem(st.key(i)));if(t)return t}return null}
  async function api(path,init={}){const h=new Headers(init.headers||{});const t=token();if(t)h.set('Authorization','Bearer '+t);if(init.body)h.set('Content-Type','application/json');const r=await fetch(API+path,{...init,headers:h,cache:'no-store'});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.detail||'No se pudo cargar la información');return d}
  function cfg(){try{return {...{next:true,results:true,tables:true,news:true,store:false},...JSON.parse(localStorage.getItem(CFG)||'{}')}}catch(_){return {next:true,results:true,tables:true,news:true,store:false}}}
  function saveCfg(c){localStorage.setItem(CFG,JSON.stringify(c))}
  let state={options:[],selections:[],events:[],recent:[],news:[],loading:false,config:cfg()};

  const css=document.createElement('style');css.textContent=`
  #defe-following-home{display:none!important}
  [data-mi-defe-button="1"]{position:relative!important;overflow:visible!important;z-index:6!important;transform:translateY(-7px)!important}
  [data-mi-defe-button="1"]:before{content:'';position:absolute;left:50%;top:50%;width:62px;height:62px;transform:translate(-50%,-50%);border-radius:22px;background:#fff;box-shadow:0 7px 18px #0b3b7833;border:1px solid #dce7f3;z-index:-1}
  .md-nav-label{display:flex!important;flex-direction:column!important;align-items:center!important;justify-content:center!important;gap:1px!important;line-height:1!important;min-width:56px!important;overflow:visible!important}
  .md-nav-shield-wrap{width:42px;height:42px;border-radius:50%;display:flex;align-items:center;justify-content:center;background:#0b3b78;box-shadow:0 5px 12px #0b3b7840;border:3px solid #fff;margin-top:-9px;flex:none!important}
  .md-nav-shield{width:33px;height:33px;object-fit:contain;display:block}.md-nav-text{font-size:11px;font-weight:900;color:#0b3b78;white-space:nowrap;margin-top:2px}
  .md-page{position:fixed;inset:0;z-index:2147482000;background:#f3f6fb;color:#17365f;overflow:auto;font-family:inherit}.md-top{position:sticky;top:0;z-index:2;background:#0b3b78;color:#fff;padding:18px 16px 14px;box-shadow:0 2px 10px #0002}.md-toprow{display:flex;align-items:center;justify-content:space-between;gap:12px}.md-title{font-size:27px;font-weight:900}.md-sub{font-size:13px;opacity:.88;margin-top:3px}.md-close,.md-config{border:0;border-radius:12px;padding:10px 12px;font-weight:900}.md-close{background:#ffffff1c;color:#fff;font-size:18px}.md-config{background:#fff;color:#0b3b78}.md-body{padding:16px 16px 96px;max-width:760px;margin:auto}.md-card{background:#fff;border:1px solid #dbe4ef;border-radius:18px;padding:14px;margin-bottom:12px;box-shadow:0 2px 8px #0000000d}.md-card h3{margin:0 0 8px;font-size:18px}.md-muted{font-size:12px;color:#64748b}.md-chips{display:flex;flex-wrap:wrap;gap:7px;margin-top:8px}.md-chip{background:#eef4fb;color:#0b3b78;border-radius:999px;padding:6px 9px;font-size:12px;font-weight:900}.md-event,.md-row{border-top:1px solid #eef2f7;padding:10px 0}.md-event:first-child,.md-row:first-child{border-top:0}.md-kicker{font-size:11px;font-weight:900;color:#0b3b78}.md-main{font-size:15px;font-weight:900;margin-top:2px}.md-meta{font-size:12px;color:#64748b;margin-top:3px}.md-empty{border:1px dashed #cbd5e1;border-radius:13px;padding:12px;color:#64748b;font-size:12px}.md-panel{position:fixed;inset:0;z-index:2147482100;background:#f5f7fb;overflow:auto}.md-panel-head{position:sticky;top:0;background:#0b3b78;color:#fff;padding:16px;display:flex;align-items:center;gap:10px}.md-panel-body{padding:16px 16px 100px;max-width:760px;margin:auto}.md-sec{background:#fff;border:1px solid #dbe4ef;border-radius:16px;padding:14px;margin-bottom:12px}.md-toggle{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:9px 0;border-top:1px solid #eef2f7}.md-toggle:first-of-type{border-top:0}.md-switch{width:46px;height:26px;border-radius:999px;background:#cbd5e1;position:relative;border:0}.md-switch[data-on="1"]{background:#0b3b78}.md-switch:after{content:'';position:absolute;top:3px;left:3px;width:20px;height:20px;border-radius:50%;background:#fff;transition:.15s}.md-switch[data-on="1"]:after{left:23px}.md-league{margin:12px 0}.md-options{display:flex;flex-wrap:wrap;gap:7px;margin-top:7px}.md-option{border:1px solid #cbd5e1;border-radius:999px;background:#fff;padding:7px 10px;font-size:12px;font-weight:800}.md-option[data-on="1"]{background:#0b3b78;color:#fff;border-color:#0b3b78}.md-save{width:100%;border:0;border-radius:13px;background:#0b3b78;color:#fff;padding:13px;font-weight:900;margin-top:8px}
  `;document.head.appendChild(css);

  function decorateNav(){return true;}
  function ensureNav(){return true}
  function fmtDate(raw){if(!raw)return null;try{const [y,m,d]=raw.split('-').map(Number);return new Intl.DateTimeFormat('es-AR',{weekday:'short',day:'2-digit',month:'2-digit'}).format(new Date(y,m-1,d))}catch(_){return raw}}
  function selectionText(){return state.selections.length?`<div class="md-chips">${state.selections.map(x=>`<span class="md-chip">${esc(x.replace('|',' · '))}</span>`).join('')}</div>`:'<div class="md-empty">Todavía no elegiste ligas o categorías para seguir.</div>'}
  function nextHtml(){if(!state.config.next)return'';const list=state.events;return `<section class="md-card"><h3>Lo próximo</h3><div class="md-muted">Una línea por cada liga o categoría que seguís.</div>${list.length?list.map(e=>e.available===false?`<div class="md-event"><div class="md-kicker">${esc(e.competition)} · ${esc(e.category)}</div><div class="md-main">Sin próxima fecha publicada</div><div class="md-meta">Todavía no tenemos fixture próximo disponible para esta selección.</div></div>`:`<div class="md-event"><div class="md-kicker">${esc(e.competition)} · ${esc(e.category)}</div><div class="md-main">${esc(e.rival||'Rival a confirmar')}</div><div class="md-meta">${esc(fmtDate(e.date)||'Fecha a confirmar')} · ${esc(e.time||'Hora a confirmar')} · ${e.local===true?'Local':e.local===false?'Visitante':'Condición a confirmar'}</div></div>`).join(''):'<div class="md-empty" style="margin-top:10px">No hay selecciones configuradas.</div>'}</section>`}
  function resultHtml(){if(!state.config.results)return'';const rows=state.recent;return `<section class="md-card"><h3>Últimos resultados</h3><div class="md-muted">El último resultado de cada liga o categoría que seguís.</div>${rows.length?rows.map(r=>r.available===false?`<div class="md-row"><div class="md-kicker">${esc(r.competition)} · ${esc(r.category)}</div><div class="md-main">Sin resultado disponible</div><div class="md-meta">Todavía no encontramos un resultado final cargado para esta selección.</div></div>`:(()=>{const date=fmtDate(r.date);const meta=[r.round_name,date,r.local===true?'Local':r.local===false?'Visitante':null].filter(Boolean).join(' · ');return `<div class="md-row"><div class="md-kicker">${esc(r.competition)} · ${esc(r.category)}</div><div class="md-main">${esc(r.home||'')} <strong>${esc(r.home_score??'—')} · ${esc(r.away_score??'—')}</strong> ${esc(r.away||'')}</div><div class="md-meta">${esc(meta||'Resultado publicado')}</div></div>`})()).join(''):'<div class="md-empty" style="margin-top:10px">No hay selecciones configuradas.</div>'}</section>`}
  function tablesHtml(){if(!state.config.tables)return'';return `<section class="md-card"><h3>Mis tablas</h3><div class="md-muted">Acceso rápido a las posiciones de las competiciones que seguís.</div>${selectionText()}</section>`}
  function newsHtml(){if(!state.config.news)return'';const arr=Array.isArray(state.news)?state.news.slice(0,3):[];return `<section class="md-card"><h3>Novedades para mí</h3>${arr.length?arr.map(n=>`<div class="md-row"><div class="md-main">${esc(n.title||n.titulo||n.headline||'Novedad del Defe')}</div><div class="md-meta">${esc(n.summary||n.resumen||n.body||'')}</div></div>`).join(''):'<div class="md-empty">Las novedades relacionadas con tus intereses aparecerán acá.</div>'}</section>`}
  function storeHtml(){if(!state.config.store)return'';return `<section class="md-card"><h3>Tienda</h3><div class="md-empty">Acá vas a poder ver novedades de stock y el estado de tus pedidos.</div></section>`}
  function pageHtml(){return `<div class="md-page" id="defe-mi-defe"><header class="md-top"><div class="md-toprow"><div><div class="md-title">Mi Defe</div><div class="md-sub">Tu Defe, a tu manera.</div></div><div style="display:flex;gap:8px"><button class="md-config" data-md-config>⚙ Personalizar</button><button class="md-close" data-md-close>×</button></div></div></header><main class="md-body"><section class="md-card"><h3>Lo que sigo</h3>${selectionText()}</section>${nextHtml()}${resultHtml()}${tablesHtml()}${newsHtml()}${storeHtml()}</main></div>`}
  function open(){document.getElementById('defe-mi-defe')?.remove();const w=document.createElement('div');w.innerHTML=pageHtml();document.body.appendChild(w.firstElementChild);document.querySelector('[data-md-close]').onclick=()=>document.getElementById('defe-mi-defe')?.remove();document.querySelector('[data-md-config]').onclick=openConfig;load()}
  function openConfig(){document.getElementById('defe-mi-defe-config')?.remove();const c={...state.config};const p=document.createElement('div');p.id='defe-mi-defe-config';p.className='md-panel';p.innerHTML=`<div class="md-panel-head"><button data-back class="md-close">‹</button><strong style="font-size:21px">Personalizar Mi Defe</strong></div><div class="md-panel-body"><section class="md-sec"><h3 style="margin-top:0">Qué quiero ver</h3>${[['next','Próximos partidos'],['results','Últimos resultados'],['tables','Mis tablas'],['news','Novedades para mí'],['store','Tienda']].map(([k,l])=>`<div class="md-toggle"><span>${l}</span><button class="md-switch" data-toggle="${k}" data-on="${c[k]?'1':'0'}"></button></div>`).join('')}</section><section class="md-sec"><h3 style="margin-top:0">Ligas y categorías que sigo</h3>${state.options.length?state.options.map(g=>`<div class="md-league"><b>${esc(g.competition)}</b><div class="md-options">${(g.categories||[]).map(cat=>{const key=`${g.competition}|${cat}`,on=state.selections.includes(key);return `<button class="md-option" data-key="${esc(key)}" data-on="${on?'1':'0'}">${esc(cat)}</button>`}).join('')}</div></div>`).join(''):'<div class="md-empty">Iniciá sesión para configurar tus ligas y categorías.</div>'}<button class="md-save" data-save>Guardar configuración</button><div class="md-muted" data-msg style="margin-top:8px"></div></section></div>`;document.body.appendChild(p);p.querySelector('[data-back]').onclick=()=>p.remove();p.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=()=>{const k=b.dataset.toggle;c[k]=!c[k];b.dataset.on=c[k]?'1':'0'});p.querySelectorAll('.md-option').forEach(b=>b.onclick=()=>{const k=b.dataset.key;if(state.selections.includes(k))state.selections=state.selections.filter(x=>x!==k);else state.selections=[...state.selections,k];b.dataset.on=state.selections.includes(k)?'1':'0'});p.querySelector('[data-save]').onclick=async()=>{const msg=p.querySelector('[data-msg]');try{msg.textContent='Guardando…';saveCfg(c);if(token())await api('/api/following/me',{method:'PUT',body:JSON.stringify({selections:state.selections})});state.config={...c};await load();p.remove();renderPage()}catch(e){msg.textContent=e.message}}}
  function renderPage(){const old=document.getElementById('defe-mi-defe');if(!old)return;const scroll=old.scrollTop;const w=document.createElement('div');w.innerHTML=pageHtml();old.replaceWith(w.firstElementChild);const n=document.getElementById('defe-mi-defe');n.scrollTop=scroll;n.querySelector('[data-md-close]').onclick=()=>n.remove();n.querySelector('[data-md-config]').onclick=openConfig}
  async function load(){if(state.loading)return;state.loading=true;try{const jobs=[];if(token())jobs.push(Promise.all([api('/api/following/options'),api('/api/following/me'),api('/api/following/next?limit=30'),api('/api/following/recent?limit=30')]).then(([o,m,n,r])=>{state.options=o.competitions||[];state.selections=m.selections||[];state.events=n.events||[];state.recent=r.results||[]}));jobs.push(api('/api/news').then(x=>state.news=Array.isArray(x)?x:(x.items||x.news||[])).catch(()=>{}));await Promise.all(jobs);renderPage()}finally{state.loading=false}}
  document.addEventListener('click',e=>{const el=e.target.closest('[data-mi-defe-nav]');if(!el)return;e.preventDefault();e.stopImmediatePropagation();open()},true);
  setTimeout(ensureNav,80);
})();