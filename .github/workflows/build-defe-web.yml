name: Restaurar UI operativa 10:21

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/build-defe-web.yml'

permissions:
  contents: read
  pages: write
  id-token: write
  actions: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar artefacto exacto del build operativo 10:21
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          curl -L --fail \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/artifacts/10498124937/zip" \
            -o /tmp/operativo.zip
          rm -rf /tmp/outer /tmp/dist
          mkdir -p /tmp/outer /tmp/dist
          unzip -q /tmp/operativo.zip -d /tmp/outer
          if [ -f /tmp/outer/netlify-publish.zip ]; then
            unzip -q /tmp/outer/netlify-publish.zip -d /tmp/dist
          else
            cp -a /tmp/outer/. /tmp/dist/
          fi
          echo 'Contenido recuperado:'
          find /tmp/dist -maxdepth 2 -type f | sort
      - name: Validar artefacto histórico sin modificarlo
        run: |
          set -euo pipefail
          test -s /tmp/dist/index.html
          test -s /tmp/dist/web-mi-defe-overlay-netlify.js
          test -s /tmp/dist/availability-ui-netlify.js
          test -s /tmp/dist/web-auth-overlay-netlify.js
          test -s /tmp/dist/escudo-dsl.svg
          grep -R -F 'Mi Defe' /tmp/dist/web-mi-defe-overlay-netlify.js >/dev/null
          grep -R -F '/api/availability/admin' /tmp/dist/availability-ui-netlify.js >/dev/null
          echo 'UI_OPERATIVA_1021_VALIDADA'
      - name: Preparar GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: /tmp/dist
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Publicar artefacto exacto 10:21
        id: deployment
        uses: actions/deploy-pages@v4
