name: Restaurar UI operativa 10:21

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/build-defe-web.yml'

permissions:
  contents: read
  pages: write
  id-token: write
  actions: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar artefacto exacto del build operativo 10:21
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          curl -L --fail \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/artifacts/10498124937/zip" \
            -o /tmp/operativo.zip
          rm -rf /tmp/outer /tmp/publish
          mkdir -p /tmp/outer /tmp/publish
          unzip -q /tmp/operativo.zip -d /tmp/outer
          if [ -f /tmp/outer/netlify-publish.zip ]; then
            unzip -q /tmp/outer/netlify-publish.zip -d /tmp/publish
          else
            cp -a /tmp/outer/. /tmp/publish/
          fi
      - name: Validar estructura real del artefacto histórico
        run: |
          set -euo pipefail
          test -s /tmp/publish/_redirects
          test -s /tmp/publish/el-defe-app/index.html
          test -s /tmp/publish/el-defe-app/web-mi-defe-overlay-netlify.js
          test -s /tmp/publish/el-defe-app/availability-ui-netlify.js
          test -s /tmp/publish/el-defe-app/web-auth-overlay-netlify.js
          test -s /tmp/publish/el-defe-app/escudo-dsl.svg
          grep -F 'Mi Defe' /tmp/publish/el-defe-app/web-mi-defe-overlay-netlify.js >/dev/null
          grep -F '/api/availability/admin' /tmp/publish/el-defe-app/availability-ui-netlify.js >/dev/null
          echo 'UI_OPERATIVA_1021_VALIDADA'
      - name: Preparar GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: /tmp/publish
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Publicar artefacto exacto 10:21
        id: deployment
        uses: actions/deploy-pages@v4
