name: UI operativa + Familias

on:
  workflow_dispatch:
  push:
    paths:
      - 'web-family-onboarding.js'
      - 'web-family-laamba-level.js'
      - 'web-attendance-player-v2.js'
      - '.github/workflows/build-defe-web.yml'

permissions:
  contents: read
  pages: write
  id-token: write
  actions: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Descargar artefacto exacto del build operativo 10:21
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          curl -L --fail -H "Authorization: Bearer ${GH_TOKEN}" -H "Accept: application/vnd.github+json" -H "X-GitHub-Api-Version: 2022-11-28" "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/artifacts/10498124937/zip" -o /tmp/operativo.zip
          rm -rf /tmp/outer /tmp/recovered /tmp/pages
          mkdir -p /tmp/outer /tmp/recovered /tmp/pages
          unzip -q /tmp/operativo.zip -d /tmp/outer
          if [ -f /tmp/outer/netlify-publish.zip ]; then unzip -q /tmp/outer/netlify-publish.zip -d /tmp/recovered; else cp -a /tmp/outer/. /tmp/recovered/; fi
      - name: Restaurar tablero operativo e integrar Familias
        run: |
          set -euo pipefail
          test -s /tmp/recovered/el-defe-app/index.html
          cp -a /tmp/recovered/el-defe-app/. /tmp/pages/
          cp web-family-onboarding.js /tmp/pages/web-family-onboarding.js
          cp web-family-laamba-level.js /tmp/pages/web-family-laamba-level.js
          cp web-attendance-player-v2.js /tmp/pages/web-attendance-player-v2.js
          rm -f /tmp/pages/web-family-delete.js
          python3 - <<'PY'
          from pathlib import Path
          p=Path('/tmp/pages/index.html')
          s=p.read_text()
          s=s.replace('<script src="./web-attendance-player-filter.js"></script>','')
          s=s.replace('<script src="./web-family-delete.js"></script>','')
          family='<script src="./web-family-onboarding.js"></script>'
          laamba='<script src="./web-family-laamba-level.js"></script>'
          player='<script src="./web-attendance-player-v2.js"></script>'
          if family not in s:s=s.replace('</body>',family+'\n</body>')
          if laamba not in s:s=s.replace('</body>',laamba+'\n</body>')
          if player not in s:s=s.replace('</body>',player+'\n</body>')
          p.write_text(s)
          PY
          test -s /tmp/pages/availability-ui-netlify.js
          test -s /tmp/pages/web-mi-defe-overlay-netlify.js
          node --check /tmp/pages/web-family-onboarding.js
          node --check /tmp/pages/web-family-laamba-level.js
          node --check /tmp/pages/web-attendance-player-v2.js
          grep -F '/api/availability/family/children' /tmp/pages/web-family-onboarding.js >/dev/null
          grep -F '/api/family/catalog' /tmp/pages/web-family-laamba-level.js >/dev/null
          grep -F '/api/availability/v2/me' /tmp/pages/web-attendance-player-v2.js >/dev/null
          grep -F '/api/availability/v2/admin' /tmp/pages/web-attendance-player-v2.js >/dev/null
          grep -F 'data-player-answer' /tmp/pages/web-attendance-player-v2.js >/dev/null
          if grep -F 'web-family-delete.js' /tmp/pages/index.html; then echo 'ERROR: overlay duplicado de familias sigue cargado'; exit 1; fi
          if grep -F 'web-attendance-player-filter.js' /tmp/pages/index.html; then echo 'ERROR: filtro experimental sigue cargado'; exit 1; fi
          echo 'ASISTENCIA_POR_HIJO_UI_PREPARADA'
      - name: Preparar GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: /tmp/pages
  deploy:
    # Sólo main publica producción. Las ramas de mejora validan el build sin desplegar.
    if: github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Publicar base validada + Familias
        id: deployment
        uses: actions/deploy-pages@v4
