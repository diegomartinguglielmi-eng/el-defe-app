// El Defe · Gestión de pedidos
(function(){
 const API=()=>window.EL_DEFE_API_URL||'',token=()=>localStorage.getItem('defe_token')||'',role=()=>localStorage.getItem('defe_role')||'';
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
 const money=v=>v==null?'A confirmar':new Intl.NumberFormat('es-AR',{style:'currency',currency:'ARS',maximumFractionDigits:0}).format(v);
 const labels={pending:'Pendiente',confirmed:'Confirmado',ready:'Listo para entregar',delivered:'Entregado',cancelled:'Cancelado'};
 async function api(path,opts={}){opts.headers={...(opts.headers||{}),Authorization:'Bearer '+token()};const r=await fetch(API()+path,{...opts,cache:'no-store'}),j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.detail||'Error');return j}
 async function render(){if(!['admin','delegado'].includes(role()))return;const parent=document.getElementById('storeAdminCard');if(!parent)return;let card=document.getElementById('storeOrdersCard');if(!card){card=document.createElement('div');card.id='storeOrdersCard';card.style.marginTop='14px';parent.appendChild(card)}card.innerHTML='<div class="meta">Cargando pedidos…</div>';try{const rows=await api('/api/store/admin/orders');card.innerHTML=`<div style="padding-top:12px;border-top:1px solid var(--line)"><div class="row"><div><b>Pedidos</b><div class="meta">Confirmá stock y seguí la entrega desde acá.</div></div><span class="badge">${rows.filter(x=>x.status==='pending').length} PENDIENTES</span></div>${rows.length?rows.map(o=>`<div class="card" style="margin:10px 0 0;padding:11px"><div class="row"><div><b>#${o.id} · ${esc(o.buyer_name)}</b><div class="meta">${esc(o.buyer_phone)}${o.buyer_category?' · '+esc(o.buyer_category):''}</div></div><span class="badge">${esc(labels[o.status]||o.status)}</span></div><div class="meta" style="margin-top:7px">${o.items.map(i=>`${i.qty}× ${esc(i.name)} · ${esc(i.size)}`).join('<br>')}</div><div class="row" style="margin-top:8px"><b>${money(o.total)}</b><span class="meta">${o.stock_applied?'Stock descontado':'Stock sin descontar'}</span></div><select data-order-status="${o.id}" style="margin:8px 0 0">${Object.entries(labels).map(([v,l])=>`<option value="${v}" ${v===o.status?'selected':''}>${l}</option>`).join('')}</select><div data-order-msg="${o.id}" class="meta"></div></div>`).join(''):'<div class="meta" style="margin-top:8px">Todavía no hay pedidos registrados.</div>'}</div>`;card.querySelectorAll('[data-order-status]').forEach(sel=>sel.onchange=async()=>{const msg=card.querySelector(`[data-order-msg="${sel.dataset.orderStatus}"]`);msg.textContent='Actualizando…';try{await api(`/api/store/admin/orders/${sel.dataset.orderStatus}/status`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:sel.value})});await render()}catch(e){msg.textContent=e.message}})}catch(e){card.innerHTML=`<div class="meta">${esc(e.message)}</div>`}}
 function init(){const mo=new MutationObserver(()=>{if(document.getElementById('storeAdminCard')&&!document.getElementById('storeOrdersCard'))render()});mo.observe(document.body,{childList:true,subtree:true});setTimeout(render,900);window.defeLoadStoreOrders=render}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();