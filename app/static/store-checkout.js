// El Defe · Checkout comercial Tienda
(function(){
  const CART_KEY='defe_store_cart_v1',BUYER_KEY='defe_store_buyer_v1';
  const apiBase=()=>window.EL_DEFE_API_URL||'';
  const money=v=>v==null?'Precio a confirmar':new Intl.NumberFormat('es-AR',{style:'currency',currency:'ARS',maximumFractionDigits:0}).format(v);
  const readCart=()=>{try{return JSON.parse(localStorage.getItem(CART_KEY)||'[]')}catch{return []}};
  const readBuyer=()=>{try{return JSON.parse(localStorage.getItem(BUYER_KEY)||'{}')}catch{return {}}};
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  async function products(){const r=await fetch(apiBase()+'/api/store/products',{cache:'no-store'});return r.ok?r.json():[];}
  function ensureStyles(){if(document.getElementById('storeCheckoutStyles'))return;const s=document.createElement('style');s.id='storeCheckoutStyles';s.textContent='.store-buyer-grid{display:grid;gap:8px}.store-buyer-grid input,.store-buyer-grid textarea{margin:0}.store-order-total{font-size:19px;font-weight:1000;color:var(--c2)}';document.head.appendChild(s);}
  function injectBuyer(){const box=document.getElementById('storeCartBody');if(!box||document.getElementById('storeBuyerCard')||!readCart().length)return;const b=readBuyer(),card=document.createElement('div');card.className='card';card.id='storeBuyerCard';card.innerHTML=`<div class="row"><b>Tus datos</b><span class="badge">PEDIDO</span></div><div class="meta">Los usamos solo para identificar este pedido por WhatsApp.</div><div class="store-buyer-grid" style="margin-top:10px"><input id="storeBuyerName" placeholder="Nombre y apellido" value="${esc(b.name||'')}"><input id="storeBuyerPhone" inputmode="tel" placeholder="Teléfono" value="${esc(b.phone||'')}"><input id="storeBuyerCategory" placeholder="Categoría / equipo (opcional)" value="${esc(b.category||'')}"><textarea id="storeBuyerNote" placeholder="Observaciones (opcional)">${esc(b.note||'')}</textarea></div><div id="storeBuyerMsg" class="meta"></div>`;
    const summary=[...box.querySelectorAll('.card')].find(x=>x.textContent.includes('Resumen'));
    if(summary)box.insertBefore(card,summary);else box.appendChild(card);
    ['storeBuyerName','storeBuyerPhone','storeBuyerCategory','storeBuyerNote'].forEach(id=>document.getElementById(id)?.addEventListener('input',saveBuyer));
    enhanceSummary();
  }
  function saveBuyer(){localStorage.setItem(BUYER_KEY,JSON.stringify({name:document.getElementById('storeBuyerName')?.value.trim()||'',phone:document.getElementById('storeBuyerPhone')?.value.trim()||'',category:document.getElementById('storeBuyerCategory')?.value.trim()||'',note:document.getElementById('storeBuyerNote')?.value.trim()||''}));}
  async function enhanceSummary(){const box=document.getElementById('storeCartBody');if(!box)return;const ps=await products(),cart=readCart();let total=0,complete=true;cart.forEach(i=>{const p=ps.find(x=>String(x.id)===String(i.id)||x.slug===i.id);if(p?.price==null)complete=false;else total+=p.price*i.qty;});const summary=[...box.querySelectorAll('.card')].find(x=>x.textContent.includes('Resumen'));if(!summary)return;let t=summary.querySelector('.store-order-total');if(!t){t=document.createElement('div');t.className='store-order-total';summary.insertBefore(t,summary.querySelector('button'));}t.textContent=complete?`Total: ${money(total)}`:'Total: a confirmar';const btn=summary.querySelector('button');if(btn)btn.textContent='Enviar pedido por WhatsApp';}
  async function checkout(){saveBuyer();const buyer=readBuyer(),cart=readCart();if(!cart.length)return;if(!buyer.name||!buyer.phone){injectBuyer();const m=document.getElementById('storeBuyerMsg');if(m)m.textContent='Completá nombre y teléfono para enviar el pedido.';document.getElementById('storeBuyerName')?.scrollIntoView({behavior:'smooth',block:'center'});return;}const ps=await products(),lines=['Hola, quiero hacer un pedido en Tienda El Defe:','',`Nombre: ${buyer.name}`,`Teléfono: ${buyer.phone}`];if(buyer.category)lines.push(`Categoría/equipo: ${buyer.category}`);lines.push('','Pedido:');let total=0,complete=true;cart.forEach(i=>{const p=ps.find(x=>String(x.id)===String(i.id)||x.slug===i.id),price=p?.price;if(price==null)complete=false;else total+=price*i.qty;lines.push(`• ${i.qty} x ${p?.name||i.id} · Talle ${i.size}${price!=null?' · '+money(price*i.qty):' · precio a confirmar'}`);});lines.push('',complete?`Total: ${money(total)}`:'Total: a confirmar');if(buyer.note)lines.push(`Observaciones: ${buyer.note}`);lines.push('','¿Me confirman stock y forma de retiro/entrega?');window.open('https://wa.me/?text='+encodeURIComponent(lines.join('\n')),'_blank','noopener');}
  function wrap(){ensureStyles();const oldOpen=window.defeOpenCart;if(typeof oldOpen==='function'&&!oldOpen.__checkout){const w=function(){const r=oldOpen.apply(this,arguments);setTimeout(injectBuyer,100);return r};w.__checkout=true;window.defeOpenCart=w;}window.defeStoreCheckout=checkout;const oldShow=window.show;if(typeof oldShow==='function'&&!oldShow.__checkout){const w=function(id){const r=oldShow.apply(this,arguments);if(id==='storeCart')setTimeout(injectBuyer,150);return r};w.__checkout=true;window.show=w;}}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(wrap,700),{once:true});else setTimeout(wrap,700);
})();